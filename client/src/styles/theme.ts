import { createTheme } from '@material-ui/core';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fdc500',
    },
    secondary: {
      main: '#00509d',
    },
  },
});

export default theme;
